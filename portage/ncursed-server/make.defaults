CFLAGS="${CFLAGS} -O2 -pipe -march=native -fno-stack-protector -U_FORTIFY_SOURCE -finline-functions -floop-interchange -floop-strip-mine -floop-block -ftree-loop-im -ftree-loop-ivcanon -fivopts"
CPPFLAGS="${CFLAGS}"
CXXFLAGS="${CFLAGS}"
LDFLAGS="${LDFLAGS} -Wl,-O2"
USE="-* cscope gpg minimal mmx ncurses nptl sasl savedconfig sse sse2 ssl symlink unicode vim-syntax ${USE}"
FEATURES="${FEATURES} fixlafiles installsources news parallel-fetch sandbox splitdebug"
LINGUAS="${LINGUAS} en ru ru_RU hy hy_AM"
